<p class="text-secondary small">Hello <%= @resource.email %>!</p>

<p class="text-secondary small">Your account has been locked due to an excessive number of unsuccessful sign in attempts.</p>

<p class="text-secondary small">Click the link below to unlock your account:</p>

<p class="text-secondary small"><%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token) %></p>
