
<% @posts.each do |post| %>

  <%= link_to post.title, post_path(post) %>
  <%= image_tag(post.post_cover.url(:thumb), alt:'Image') if post.post_cover? %>

<% end %>

<%= will_paginate @posts, previous_label: "Nowsze", next_label: "Starsze" %>
